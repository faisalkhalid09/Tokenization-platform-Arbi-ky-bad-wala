{
  "name": "tokenization-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for blockchain tokenization platform",
  "main": "index.js",
  "scripts": {
    "build": "npx hardhat compile",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:mumbai": "npx hardhat run scripts/deploy.js --network mumbai",
    "deploy:bsc": "npx hardhat run scripts/deploy.js --network bscTestnet",
    "verify:mumbai": "npx hardhat verify --network mumbai",
    "verify:bsc": "npx hardhat verify --network bscTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
